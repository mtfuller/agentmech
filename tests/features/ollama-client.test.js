describe('Ollama Client Integration', () => {
  test.skip('Ollama client tests (requires running Ollama instance)', () => {
    // Tests are skipped as they require a running Ollama instance
    // To test manually, run: ai-workflow list-models
  });
});
